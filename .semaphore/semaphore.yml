version: v1.0
name: Add User
blocks:
  - name: Add User
    task:
      jobs:
        - name: Add User
          commands:
            - checkout
            - ansible-playbook -i inventory.ini add_user.yml
